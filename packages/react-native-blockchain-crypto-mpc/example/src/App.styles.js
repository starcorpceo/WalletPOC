import { StyleSheet} from "react-native";

export default styles = StyleSheet.create({
    container: {
      flex: 1,
      margin: 22,
      marginTop: 50,
    },
    row: {
      flexDirection: 'row',
      justifyContent:'flex-start',
      alignItems: 'center'
    }
  });