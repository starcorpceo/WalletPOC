datasource db {
    provider = "postgresql"
    url      = env("MAIN_DB_URL")
}

generator client {
    provider = "prisma-client-js"
}

model User {
    id     String @id @default(uuid())
    secret String
}
